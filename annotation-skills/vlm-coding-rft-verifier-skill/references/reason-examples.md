# Reason Examples

Use these as style anchors, not fixed templates. Adapt to the current visible chart evidence.

## Rubric Reasons

- image1 的雷达图主体存在，但缺少原图中的散点层，组合图类型没有完整复现
- image2 的 X 轴量程和主刻度与原图一致，但右侧图例文字有轻微截断
- image1 的主峰位置接近原图，但次峰热度区域范围偏小
- image2 的误差棒上下端点接近原图，但误差棒位置没有位于蓝色柱和橙色柱之间
- image1 的数据源说明在图表框右下角，淡黄色外框也有保留
- image2 缺少右下角数据源说明，静态辅助信息不完整

## Dimension Reasons

- O1：图表主类型和多系列折线结构都还原出来了，误差棒变体也存在
- O1：只保留雷达图，没有复现原图中的散点层，组合图意图不完整
- O2：坐标轴量程与主要趋势接近，但多个峰值位置偏移，图例单位也缺失
- O2：核心数值分布基本正确，标题和图例完整，只有个别标签位置有轻微偏差
- O3：整体布局接近原图，但图例进入绘图区遮挡数据点，右侧留白也偏小
- O3：颜色和线条粗细与原图差异明显，背景网格风格也不一致

## Pairwise Reasons

- image1 的图表类型和主要数据趋势都更接近原图，image2 虽然颜色更像但坐标轴量程改写明显，因此 image1 胜出
- image2 保留了原图的散点层和雷达结构，image1 缺少散点导致 O1 意图不完整，因此 image2 胜出
- 两张图 O1 都正确，但 image1 的关键峰值位置和图例单位更准确，image2 存在明显数据偏移，因此 image1 胜出
- image2 的整体布局更贴近原图，且没有 image1 的标签遮挡问题，在数据接近时 image2 更好

## Report Reasons

- overall 显示 image1 更高，但 image1 存在趋势反转这一主需错误，pairwise 需要 report 讨论
- 两张图总分接近，但一个在 O2 数据上更准，另一个在 O3 风格上更像，胜负依据需要标明
