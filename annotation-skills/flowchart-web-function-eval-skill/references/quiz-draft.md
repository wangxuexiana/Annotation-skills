# Quiz Draft

No concrete permission quiz questions were provided. Use the manual and training summary as evidence when quiz questions appear.

Likely facts:

- Open `modelA` and interact before judging behavior.
- Judge each rubric independently.
- Do not add unstated requirements.
- Waste/abandon only when the item cannot be evaluated.
- Screenshot correctness compares `modelA_img` with the initial loaded page before interaction.
- Overall functionality score is an integer from `0` to `10`.
